services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: criteriafilms
      POSTGRES_PASSWORD: criteriafilms
      POSTGRES_DB: criteriafilms
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
